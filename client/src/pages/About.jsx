// Import the React library (needed to use JSX)
import React from "react";

// Define a functional component called "About"
export default function About() {
  // JSX returned by the component
  return <h1>About Page</h1>;
}

// Explanation:
// - `export default` allows this component to be imported elsewhere using:
//     import About from './About';
// - This is a simple "About" page — perfect as part of a multi-page app using React Router.
